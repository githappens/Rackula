# Vendored upstream copy of the secure-coding skill (see #2425).
# Keep byte-identical to upstream; do not reformat.
.github/claude-plugins/

# Generated JSON Schema artifact. Its formatting is owned by
# `npm run generate-schema` (JSON.stringify(..., null, 2)) and verified
# byte-for-byte by src/tests/layout-json-schema.test.ts. Prettier's array
# collapsing would diverge from the generator and break that test.
static/schemas/

# Design spec assets from the M12 mobile UX brainstorm (#2467).
# The HTML mockup is a one-off design artifact, not production code.
docs/superpowers/specs/assets/

# Fork-local build output (Santa-lockdown toolchain): relocated node_modules
# and downloaded Playwright browsers live here. Never format them.
# See docs/fork/dev-environment.md.
build-deps/
build/
